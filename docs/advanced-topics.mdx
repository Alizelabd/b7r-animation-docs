---
title: Advanced Topics
description: Master scroll-linked and staggered animations
---

import { StaggerDemo } from "@/components/demos/StaggerDemo";
import { TextAnimationDemo } from "@/components/demos/TextAnimationDemo";

<div className="space-y-12">

<div className="space-y-4">
  <h1 className="text-5xl font-bold">Advanced Topics</h1>
  <p className="text-xl text-muted-foreground">
    Master powerful animation techniques
  </p>
</div>

<div className="space-y-8">
  <div className="space-y-6">
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 rounded-lg bg-blue-500/10 flex items-center justify-center">
        <span className="text-2xl">1</span>
      </div>
      <h2 className="text-3xl font-bold">Scroll-Linked Animations</h2>
    </div>

    <p className="text-muted-foreground">
      Link animation progress directly to scroll position using the <code className="text-sm font-mono bg-muted px-2 py-1 rounded">scrub</code> prop
    </p>

    <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
      <div className="space-y-3">
        <h3 className="text-lg font-semibold">Basic Scrub</h3>
        <div className="rounded-lg overflow-hidden border bg-muted/50">
          <pre className="p-4 text-sm"><code>{`<AnimateOnScroll
  scrub={true}
  once={false}
>
  <AnimationItem variant="fromLeft">
    <h1>Follows your scroll</h1>
  </AnimationItem>
</AnimateOnScroll>`}</code></pre>
        </div>
      </div>

      <div className="space-y-3">
        <h3 className="text-lg font-semibold">Smooth Scrub</h3>
        <div className="rounded-lg overflow-hidden border bg-muted/50">
          <pre className="p-4 text-sm"><code>{`<AnimateOnScroll
  scrub={1}
  once={false}
>
  <AnimationItem variant="fromRight">
    <div>Smooth catch-up</div>
  </AnimationItem>
</AnimateOnScroll>`}</code></pre>
        </div>
      </div>
    </div>

    <div className="rounded-lg border bg-card p-6">
      <h3 className="font-semibold mb-3">How It Works</h3>
      <div className="space-y-3 text-sm text-muted-foreground">
        <p>
          <strong>scrub={true}</strong> - Animation progress locked to scroll position in real-time
        </p>
        <p>
          <strong>scrub={1}</strong> - Smooth easing with 1 second catch-up time
        </p>
        <p>
          <strong>scrub={0.5}</strong> - Faster catch-up (0.5 seconds)
        </p>
      </div>
    </div>

    <div className="rounded-lg border bg-gradient-to-br from-blue-500/5 to-cyan-500/5 p-6">
      <h4 className="font-semibold mb-2">Use Cases</h4>
      <ul className="space-y-2 text-sm text-muted-foreground">
        <li className="flex items-start gap-2">
          <span className="text-primary mt-1">✓</span>
          <span>Parallax effects - Elements move at different speeds</span>
        </li>
        <li className="flex items-start gap-2">
          <span className="text-primary mt-1">✓</span>
          <span>Progress indicators - Visual scroll feedback</span>
        </li>
        <li className="flex items-start gap-2">
          <span className="text-primary mt-1">✓</span>
          <span>Interactive storytelling - Scroll-responsive content</span>
        </li>
      </ul>
    </div>
  </div>

  <div className="space-y-6">
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 rounded-lg bg-cyan-500/10 flex items-center justify-center">
        <span className="text-2xl">2</span>
      </div>
      <h2 className="text-3xl font-bold">Staggered Animations</h2>
    </div>

    <p className="text-muted-foreground">
      Create cascading wave-like effects with the <code className="text-sm font-mono bg-muted px-2 py-1 rounded">stagger</code> prop
    </p>

    <StaggerDemo />

    <div className="rounded-lg border bg-card p-6">
      <h3 className="font-semibold mb-4">Implementation</h3>
      <div className="rounded-lg overflow-hidden border bg-muted/50">
        <pre className="p-4 overflow-x-auto text-sm"><code>{`<AnimateOnScroll>
  {items.map((item, index) => (
    <AnimationItem
      key={item.id}
      variant="pop"
      stagger={index * 0.1}
    >
      {item.content}
    </AnimationItem>
  ))}
</AnimateOnScroll>`}</code></pre>
      </div>
    </div>

    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
      <div className="rounded-lg border bg-card p-4">
        <h4 className="font-semibold mb-2 text-sm">Small Delay</h4>
        <code className="text-xs bg-muted px-2 py-1 rounded">stagger={0.05}</code>
        <p className="text-xs text-muted-foreground mt-2">Quick cascade effect</p>
      </div>

      <div className="rounded-lg border bg-card p-4">
        <h4 className="font-semibold mb-2 text-sm">Medium Delay</h4>
        <code className="text-xs bg-muted px-2 py-1 rounded">stagger={0.1}</code>
        <p className="text-xs text-muted-foreground mt-2">Balanced wave effect</p>
      </div>

      <div className="rounded-lg border bg-card p-4">
        <h4 className="font-semibold mb-2 text-sm">Large Delay</h4>
        <code className="text-xs bg-muted px-2 py-1 rounded">stagger={0.2}</code>
        <p className="text-xs text-muted-foreground mt-2">Dramatic effect</p>
      </div>
    </div>
  </div>

  <div className="space-y-6">
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 rounded-lg bg-green-500/10 flex items-center justify-center">
        <span className="text-2xl">3</span>
      </div>
      <h2 className="text-3xl font-bold">Text Animations</h2>
    </div>

    <p className="text-muted-foreground">
      Animate text by splitting into characters, words, or lines
    </p>

    <TextAnimationDemo />

    <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
      <div className="space-y-3">
        <h3 className="text-lg font-semibold">By Characters</h3>
        <div className="rounded-lg overflow-hidden border bg-muted/50">
          <pre className="p-4 text-sm"><code>{`<AnimateText
  type="chars"
  variant="pop"
>
  Character by character
</AnimateText>`}</code></pre>
        </div>
        <p className="text-xs text-muted-foreground">Best for short headers</p>
      </div>

      <div className="space-y-3">
        <h3 className="text-lg font-semibold">By Words</h3>
        <div className="rounded-lg overflow-hidden border bg-muted/50">
          <pre className="p-4 text-sm"><code>{`<AnimateText
  type="words"
  variant="fromBottom"
>
  Word by word effect
</AnimateText>`}</code></pre>
        </div>
        <p className="text-xs text-muted-foreground">Best for sentences</p>
      </div>

      <div className="space-y-3">
        <h3 className="text-lg font-semibold">By Lines</h3>
        <div className="rounded-lg overflow-hidden border bg-muted/50">
          <pre className="p-4 text-sm"><code>{`<AnimateText
  type="lines"
  variant="fromLeft"
>
  Line by line
  animation effect
</AnimateText>`}</code></pre>
        </div>
        <p className="text-xs text-muted-foreground">Best for paragraphs</p>
      </div>
    </div>
  </div>

  <div className="space-y-6">
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 rounded-lg bg-purple-500/10 flex items-center justify-center">
        <span className="text-2xl">4</span>
      </div>
      <h2 className="text-3xl font-bold">Debugging</h2>
    </div>

    <p className="text-muted-foreground">
      Use the <code className="text-sm font-mono bg-muted px-2 py-1 rounded">debug</code> prop to visualize scroll trigger markers
    </p>

    <div className="rounded-lg overflow-hidden border bg-muted/50">
      <pre className="p-4"><code className="text-sm">{`<AnimateOnScroll debug={true}>
  {/* Your animated content */}
</AnimateOnScroll>`}</code></pre>
    </div>

    <div className="rounded-lg border bg-card p-6">
      <h3 className="font-semibold mb-3">What Debug Shows</h3>
      <div className="space-y-2 text-sm text-muted-foreground">
        <p>
          <strong className="text-foreground">Green line</strong> - Trigger start position
        </p>
        <p>
          <strong className="text-foreground">Red line</strong> - Trigger end position
        </p>
        <p>
          <strong className="text-foreground">Playhead</strong> - Current scroll position
        </p>
      </div>
    </div>
  </div>

  <div className="space-y-6">
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 rounded-lg bg-amber-500/10 flex items-center justify-center">
        <span className="text-2xl">5</span>
      </div>
      <h2 className="text-3xl font-bold">Performance Tips</h2>
    </div>

    <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div className="rounded-lg border bg-gradient-to-br from-green-500/5 to-emerald-500/5 p-6">
        <h4 className="font-semibold mb-3 text-green-700">Do</h4>
        <ul className="space-y-2 text-sm text-muted-foreground">
          <li className="flex items-start gap-2">
            <span className="text-green-600 mt-1">✓</span>
            <span>Use GPU properties (transform, opacity)</span>
          </li>
          <li className="flex items-start gap-2">
            <span className="text-green-600 mt-1">✓</span>
            <span>Use once={true} (default)</span>
          </li>
          <li className="flex items-start gap-2">
            <span className="text-green-600 mt-1">✓</span>
            <span>Stagger moderate amounts</span>
          </li>
        </ul>
      </div>

      <div className="rounded-lg border bg-gradient-to-br from-red-500/5 to-rose-500/5 p-6">
        <h4 className="font-semibold mb-3 text-red-700">Avoid</h4>
        <ul className="space-y-2 text-sm text-muted-foreground">
          <li className="flex items-start gap-2">
            <span className="text-red-600 mt-1">✗</span>
            <span>Animating width/height</span>
          </li>
          <li className="flex items-start gap-2">
            <span className="text-red-600 mt-1">✗</span>
            <span>Heavy stagger (>0.3s)</span>
          </li>
          <li className="flex items-start gap-2">
            <span className="text-red-600 mt-1">✗</span>
            <span>Too many animations on page</span>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>

<div className="rounded-xl bg-gradient-to-r from-blue-500/10 to-cyan-500/10 border border-blue-500/20 p-8">
  <h3 className="text-2xl font-bold mb-4">Continue Learning</h3>
  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
    <a href="./variants" className="flex flex-col p-4 rounded-lg bg-background/50 hover:bg-background transition-colors border">
      <span className="font-semibold mb-1">Animation Variants</span>
      <span className="text-sm text-muted-foreground">Explore all presets</span>
    </a>
    <a href="./animation-builder-showcase" className="flex flex-col p-4 rounded-lg bg-background/50 hover:bg-background transition-colors border">
      <span className="font-semibold mb-1">Animation Builder</span>
      <span className="text-sm text-muted-foreground">Test and experiment</span>
    </a>
  </div>
</div>

</div>
